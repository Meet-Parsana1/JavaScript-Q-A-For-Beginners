// WHICH OF THE FOLLOWING IS USED TO LOOK FOR THE FARTHEST ANCESTOR THAT MATCHES A GIVEN CSS SELECTOR ?

// ANSWER: closest()

// The `closest()` method is used to look for the nearest ancestor that matches a given CSS selector.

// It traverses the DOM tree upwards from the current element until it finds a match or reaches the root of the document. If no matching ancestor is found, it returns `null`.

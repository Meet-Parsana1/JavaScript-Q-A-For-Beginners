// Use logical operators to find whether the age of a person lies between 10 and 20.

// Let's set a person's age
let age = 13;

// Check if the age is more than 10 AND less than 20
// && means "and" in JavaScript — both conditions must be true
if (age > 10 && age < 20) {
      // console.log() is a built-in function in JavaScript that is used to print output to the console.
      console.log("Your age lies between 10 and 20.");
} else {
      // This runs if the "if statement" becomes false
      console.log("Your age does not lie between 10 and 20.");
}






